# Factory Job: {name}

- Job ID: `{job_id}`
- Mode: `{mode}`
- Created: `{created_at}`
- Current state: `NEW`

This is a private factory work order. Read `status.json` before continuing and
re-check any external state that may have changed.
